# Copy to /opt/desk-atlas/env on the Pi (chmod 600). Never commit real values.
# Ethan places XAI_API_KEY himself (secure card / local paste on Pi — never chat).
XAI_API_KEY=
ATLAS_SPEAK_PORT=8080
ATLAS_PLAY=1
ATLAS_VOICE=eve
# Optional ALSA device once WhisPlay is up, e.g. plughw:wm8960soundcard
# ATLAS_APLAY_DEVICE=
