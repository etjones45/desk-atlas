# Named Cloudflare tunnel template (replace placeholders after Zero Trust tunnel exists).
# Quick tunnel is Phase 0 only; Phase 2+ prefers named hostname.
#
# tunnel: <TUNNEL_UUID>
# credentials-file: /opt/desk-atlas/cloudflared/<TUNNEL_UUID>.json
#
# ingress:
#   - hostname: desk-atlas.example.com
#     service: http://127.0.0.1:8080
#   - service: http_status:404
